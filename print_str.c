#include "main.h"

/**
 * _strlen - Computes the length of a string.
 * @str: Input string.
 *
 * Return: Length of the string.
 */
int _strlen(char *str)
{
	int f;

	for (f = 0; str[f] != 0; f++)
	{
		/* Loop through the string characters */
	}

	return (f);
}

/**
 * _strlenc - Computes the length of a constant string.
 * @str: Input constant string.
 *
 * Return: Length of the constant string.
 */
int _strlenc(const char *str)
{
	int f;

	for (f = 0; str[f] != 0; f++)
	{
		/* Loop through the constant string characters */
	}

	return (f);
}
